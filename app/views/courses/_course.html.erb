<li>
    
    <div class="row">
        <div class="course">

            <div>
                <%= image_tag(course.main_video_id, :alt => "Image of game") %>
            </div>

            <div class="course_info">
                <h1><%= link_to course.name, course %></h1>
                <p><%= course.description %></p>
            </div>

        </div>
    </div>

</li>